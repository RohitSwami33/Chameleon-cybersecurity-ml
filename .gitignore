# ===========================================================================
# Chameleon Adaptive Deception System — .gitignore
# ===========================================================================

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ── Virtual Environment ──────────────────────────────────────────────────────
venv/
.venv/
ENV/
env/

# ── Test / CI Cache ──────────────────────────────────────────────────────────
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# ── Environment / Secrets  (CRITICAL — never commit) ────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── IDE ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Node / Frontend build ────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
frontend/dist/
frontend/build/

# ── MongoDB runtime data ─────────────────────────────────────────────────────
Backend/data/db/
Backend/data/*.log*

# ── Solidity / Blockchain compiled artifacts ─────────────────────────────────
Backend/contracts/*.json

# ── Training logs & transient metrics (not source-controlled) ───────────────
training_output*.log

# ── Large model files (download separately or use Git LFS) ──────────────────
Backend/chamaeleon-4bit-balanced/
Backend/chamaeleon-4bit/
finetuning-model/chamaeleon-4bit-balanced/
finetuning-model/chamaeleon-4bit/
finetuning-model/adapters*/

# ===========================================================================
# NOTE: The following file types are NOT ignored here — they are tracked via
# Git LFS (see .gitattributes).  Do NOT add them back to this list.
#
#   *.safetensors  — MLX / Hugging Face model shards
#   *.bin          — PyTorch / binary model blobs
#   *.npz          — NumPy compressed arrays (MLX weights)
#   *.jsonl        — training datasets
#   *.csv          — labelled command datasets
#   *.pth / *.pt   — PyTorch checkpoint files  (pushed via LFS)
#   *.pkl          — tokenizer / scaler pickles (pushed via LFS)
#   *.keras / *.h5 — Keras model files          (pushed via LFS)
# ===========================================================================
