services:
  # Combined Backend + Frontend Service
  - type: web
    name: chameleon-system
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: "pip install --upgrade pip && pip install -r Backend/requirements.txt && cd frontend && npm install && npm run build && cd .."
    startCommand: "cd Backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: MONGODB_URL
        value: mongodb+srv://privatestudent33_db_user:hk3rc71C0GsEoJf4@cluster0.uiklaos.mongodb.net/chameleon_db?retryWrites=true&w=majority&tls=true
      - key: DATABASE_NAME
        value: chameleon_db
      - key: GEMINI_API_KEY
        value: AIzaSyB7w5tQXvg1D7cVuqpeR6cZ5OMzNKCqguY
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 60
