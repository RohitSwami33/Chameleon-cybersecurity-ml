# Frontend-Backend Integration Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    http://localhost:5174                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP/HTTPS Requests
                         │ + JWT Token
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Components:                                              │  │
│  │  • Login.jsx                                              │  │
│  │  • Dashboard.jsx                                          │  │
│  │  • AIChatbot.jsx                                          │  │
│  │  • ChatbotPage.jsx                                        │  │
│  │  • Navbar.jsx                                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Services:                                                │  │
│  │  • api.js (Axios + Interceptors)                          │  │
│  │    - Auto JWT injection                                   │  │
│  │    - Auto 401 handling                                    │  │
│  │    - CORS handling                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ API Calls
                         │ Authorization: Bearer <token>
                         │ Origin: http://localhost:5174
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                 CORS MIDDLEWARE (FastAPI)                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  • Validates Origin                                       │  │
│  │  • Checks Credentials                                     │  │
│  │  • Handles Preflight (OPTIONS)                            │  │
│  │  • Adds CORS Headers                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                BACKEND API (FastAPI + Python)                    │
│                   http://localhost:8000                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Public Endpoints:                                        │  │
│  │  • POST /api/auth/login                                   │  │
│  │  • GET  /api/health                                       │  │
│  │  • POST /api/trap/submit                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Protected Endpoints (JWT Required):                      │  │
│  │  • GET  /api/dashboard/stats                              │  │
│  │  • GET  /api/dashboard/logs                               │  │
│  │  • POST /api/chatbot/chat                                 │  │
│  │  • GET  /api/chatbot/history                              │  │
│  │  • POST /api/chatbot/analyze-attack/{id}                  │  │
│  │  • GET  /api/threat-intel/reports                         │  │
│  │  • GET  /api/blockchain/verify                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Services:                                                │  │
│  │  • chatbot_service.py (Gemini AI)                         │  │
│  │  • ml_classifier.py (TensorFlow)                          │  │
│  │  • blockchain_logger.py                                   │  │
│  │  • threat_intel_service.py                                │  │
│  │  • deception_engine.py                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   MongoDB    │  │  Gemini AI   │  │ DuckDuckGo   │
│   Database   │  │     API      │  │    Search    │
└──────────────┘  └──────────────┘  └──────────────┘
```

## 🔄 Request Flow: Chatbot Message

```
1. USER ACTION
   └─> User types message in chatbot
       └─> Clicks send button

2. FRONTEND (AIChatbot.jsx)
   └─> handleSend() function triggered
       └─> api.post('/api/chatbot/chat', { message, use_search })

3. AXIOS INTERCEPTOR (api.js)
   └─> Adds Authorization header
       └─> Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
       └─> Adds Origin header
           └─> Origin: http://localhost:5174

4. BROWSER
   └─> Sends OPTIONS request (preflight)
       └─> Backend responds with CORS headers
           └─> Sends actual POST request

5. BACKEND CORS MIDDLEWARE
   └─> Validates origin
       └─> Checks credentials
           └─> Adds CORS headers to response

6. BACKEND AUTH MIDDLEWARE
   └─> Verifies JWT token
       └─> Extracts username
           └─> Passes to endpoint

7. CHATBOT ENDPOINT (/api/chatbot/chat)
   └─> Gets chatbot instance
       └─> Calls chatbot.chat(message, use_search)

8. CHATBOT SERVICE
   └─> Builds prompt with context
       └─> If use_search: Search DuckDuckGo
           └─> Calls Gemini AI API
               └─> Formats response

9. RESPONSE FLOW
   └─> Backend sends JSON response
       └─> CORS headers included
           └─> Frontend receives response
               └─> Updates UI with bot message

10. ERROR HANDLING
    └─> If 401: Redirect to login
        └─> If 500: Show error message
            └─> If network error: Show retry option
```

## 🔐 Authentication Flow

```
┌─────────────┐
│   Login     │
│   Page      │
└──────┬──────┘
       │
       │ 1. User enters credentials
       │    username: admin
       │    password: chameleon2024
       │
       ▼
┌─────────────────────────────────────┐
│  POST /api/auth/login               │
│  { username, password }             │
└──────┬──────────────────────────────┘
       │
       │ 2. Backend validates credentials
       │
       ▼
┌─────────────────────────────────────┐
│  JWT Token Generated                │
│  {                                  │
│    "sub": "admin",                  │
│    "exp": 1700000000                │
│  }                                  │
└──────┬──────────────────────────────┘
       │
       │ 3. Token returned to frontend
       │
       ▼
┌─────────────────────────────────────┐
│  localStorage.setItem(              │
│    'authToken',                     │
│    'eyJhbGciOiJIUzI1NiIs...'        │
│  )                                  │
└──────┬──────────────────────────────┘
       │
       │ 4. Redirect to dashboard
       │
       ▼
┌─────────────────────────────────────┐
│  All subsequent requests include:   │
│  Authorization: Bearer <token>      │
└─────────────────────────────────────┘
```

## 📡 CORS Preflight Example

```
REQUEST (OPTIONS):
┌─────────────────────────────────────────────┐
│ OPTIONS /api/chatbot/chat HTTP/1.1          │
│ Host: localhost:8000                        │
│ Origin: http://localhost:5174               │
│ Access-Control-Request-Method: POST         │
│ Access-Control-Request-Headers:             │
│   authorization, content-type               │
└─────────────────────────────────────────────┘
                    │
                    ▼
RESPONSE (200 OK):
┌─────────────────────────────────────────────┐
│ HTTP/1.1 200 OK                             │
│ Access-Control-Allow-Origin:                │
│   http://localhost:5174                     │
│ Access-Control-Allow-Methods:               │
│   GET, POST, PUT, DELETE, OPTIONS, PATCH    │
│ Access-Control-Allow-Headers: *             │
│ Access-Control-Allow-Credentials: true      │
│ Access-Control-Max-Age: 600                 │
└─────────────────────────────────────────────┘
                    │
                    ▼
ACTUAL REQUEST (POST):
┌─────────────────────────────────────────────┐
│ POST /api/chatbot/chat HTTP/1.1             │
│ Host: localhost:8000                        │
│ Origin: http://localhost:5174               │
│ Authorization: Bearer eyJhbGciOiJIUzI1NiIs  │
│ Content-Type: application/json              │
│                                             │
│ {"message":"test","use_search":false}       │
└─────────────────────────────────────────────┘
```

## 🎯 Data Flow: Dashboard Stats

```
Dashboard Component
       │
       │ useEffect(() => fetchData())
       │
       ▼
getDashboardStats()
       │
       │ api.get('/api/dashboard/stats')
       │
       ▼
Axios Interceptor
       │
       │ + Authorization: Bearer <token>
       │
       ▼
Backend Endpoint
       │
       │ verify_token(token)
       │
       ▼
Database Query
       │
       │ MongoDB aggregation
       │
       ▼
Response
       │
       │ {
       │   total_attempts: 150,
       │   malicious_attempts: 45,
       │   attack_distribution: {...},
       │   geo_locations: [...]
       │ }
       │
       ▼
Frontend Update
       │
       │ setStats(response.data)
       │
       ▼
UI Renders
```

## 🔧 Configuration Files

```
Project Root
│
├── Backend/
│   ├── main.py                 ← CORS config
│   ├── config.py               ← API keys, settings
│   ├── chatbot_service.py      ← Gemini AI integration
│   └── requirements.txt        ← Python dependencies
│
├── frontend/
│   ├── .env                    ← API URL config
│   ├── src/
│   │   ├── services/
│   │   │   └── api.js          ← Axios config
│   │   ├── components/
│   │   │   ├── AIChatbot.jsx   ← Chatbot UI
│   │   │   └── Navbar.jsx      ← Navigation
│   │   └── pages/
│   │       └── ChatbotPage.jsx ← Chatbot page
│   └── package.json            ← Node dependencies
│
└── test_integration.sh         ← Integration tests
```

## 📊 Performance Metrics

```
Typical Response Times:
┌────────────────────────────┬──────────────┐
│ Endpoint                   │ Response Time│
├────────────────────────────┼──────────────┤
│ /api/health                │ < 50ms       │
│ /api/auth/login            │ 100-200ms    │
│ /api/dashboard/stats       │ 200-500ms    │
│ /api/chatbot/chat          │ 2-5s         │
│ /api/chatbot/chat (search) │ 5-10s        │
└────────────────────────────┴──────────────┘
```

## 🚀 Deployment Considerations

### Development
- CORS: Allow all origins (*)
- HTTP: localhost
- No SSL required

### Production
- CORS: Specific origins only
- HTTPS: Required
- SSL certificates
- Environment variables
- Rate limiting
- CDN for frontend
- Load balancer for backend

---

**Architecture Status**: ✅ Fully Integrated
**Last Updated**: November 23, 2025
